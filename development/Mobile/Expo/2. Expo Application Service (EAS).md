# What is it?
This is an optional set of services that complements Expo, the framework, in each step of the development process.

So, you can see this service as an option for delegate tasks that are time consuming.
