It seems the model don't use (or decide to no use) a bind tool when you are using it in structure output.

Split the workflow into 2 agents.
1. The first one for data retrieval
2. The second one for responsing.

If you are implementing APE technique, it's better to focus in few fields of your structure rather than trying to implement all at once.

Use enums can help if there is a set of options on which the model can fit.